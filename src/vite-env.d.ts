/// <reference types="vite/client" />

// Injected by Vite's `define` (see vite.config.ts / electron.vite.config.ts)
// from package.json's `version` field - the build-time fallback for
// src/utils/appVersion.ts. See that file for the full rationale.
declare const __APP_VERSION__: string;
